#include <inttypes.h>

// calculate the sine function with an taylorExpansion
float SinusFunction(float);
// own FactorialFunction
float FactorialFunction(uint64_t);

// own powerFunction
float powerFunction(float,uint8_t);

